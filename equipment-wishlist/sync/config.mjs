// Deliberately unconfigured: never guess an existing project or silently open a database.
// Replace only after project approval, Google sign-in setup and tested rules deployment.
// Firebase web app config is public metadata, NOT a service-account key.
export const sharedConfig = null;
