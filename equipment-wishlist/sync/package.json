{
  "name": "mh-wishlist-shared-verification",
  "private": true,
  "type": "module",
  "scripts": {
    "test:core": "node --test core.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-mh-wishlist --config firebase.json \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.2"
  }
}
